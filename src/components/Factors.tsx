import React from 'react';
import MainLayout from './MainLayout';

const Factors: React.FC = () => {
  return (
    <MainLayout title="Factores (provisional)" showCta={false}>
      <div style={{ color: 'white', textAlign: 'center' }}>
        <h1>Factores (provisional)</h1>
        <p>Esta es una página provisional llamada "factors" con la misma estructura base.</p>
      </div>
    </MainLayout>
  );
};

export default Factors;
